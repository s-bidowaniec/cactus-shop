// selectors

// actions

//action creators

// thunk actions

//reducer
const productsReducer = (statePart = null, action) => {
  switch (action.type) {
    default:
      return statePart;
  }
};
export default productsReducer;