// selectors

// actions

//action creators

// thunk actions

//reducer
const ordersReducer = (statePart = null, action) => {
  switch (action.type) {
    default:
      return statePart;
  }
};
export default ordersReducer;